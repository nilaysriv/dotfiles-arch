# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                          Animation Configuration                             ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

animations {
    enabled = false
    
    # Animation curves:
    # default, linear, easeIn, easeOut, easeInOut
    # easeInSine, easeOutSine, easeInOutSine
    # easeInQuad, easeOutQuad, easeInOutQuad
    # easeInCubic, easeOutCubic, easeInOutCubic
    # easeInQuart, easeOutQuart, easeInOutQuart
    # easeInQuint, easeOutQuint, easeInOutQuint
    # easeInExpo, easeOutExpo, easeInOutExpo
    # easeInCirc, easeOutCirc, easeInOutCirc
    # easeInBack, easeOutBack, easeInOutBack
    
    # Fast bezier curves for snappy animations
    bezier = fast, 0.1, 0.0, 0.0, 1.0
    bezier = snap, 0.0, 0.0, 0.2, 1.0
    bezier = smooth, 0.4, 0.0, 0.2, 1.0
    
    # Window animations - simplified for performance
    animation = windows, 1, 2, default, slide
    animation = windowsIn, 1, 2, default, slide
    animation = windowsOut, 1, 2, default, slide
    animation = windowsMove, 1, 2, default, slide
    
    # Border color animations - instant
    animation = border, 1, 1, default
    animation = borderangle, 1, 1, default
    
    # Fade animations - simplified
    animation = fade, 1, 2, default
    animation = fadeIn, 1, 2, default
    animation = fadeOut, 1, 2, default
    animation = fadeSwitch, 1, 2, default
    animation = fadeShadow, 1, 2, default
    animation = fadeDim, 1, 2, default
    
    # Workspace animations - simplified
    animation = workspaces, 1, 3, default, slide
    animation = specialWorkspace, 1, 3, default, slidevert
}
